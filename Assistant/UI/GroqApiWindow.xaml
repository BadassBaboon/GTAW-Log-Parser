<Controls:MetroWindow x:Class="Assistant.UI.GroqApiWindow"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:local="clr-namespace:Assistant.UI"
                      mc:Ignorable="d"
                      WindowStartupLocation="CenterOwner"
                      ResizeMode="NoResize"
                      BorderThickness="0"
                      GlowBrush="Black"
                      Title="Manage Groq API Keys" Height="360" Width="550" ShowInTaskbar="True" IsMinButtonEnabled="False" IsMaxRestoreButtonEnabled="False" ShowIconOnTitleBar="False" TitleCharacterCasing="Normal" Icon="../Resources/AppIcon.ico">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top link & description -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Get your API keys from the " VerticalAlignment="Center"/>
            <TextBlock x:Name="GroqConsoleLink" Text="Groq Console Keys Page" Foreground="#007ACC" Cursor="Hand" TextDecorations="Underline" MouseLeftButtonDown="GroqConsoleLink_Click" VerticalAlignment="Center"/>
            <TextBlock Text=" (https://console.groq.com/keys)" VerticalAlignment="Center" Foreground="Gray" Margin="5,0,0,0" FontSize="11"/>
        </StackPanel>

        <!-- Key Input Row -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewKeyInput" Grid.Column="0" Controls:TextBoxHelper.Watermark="Paste your Groq API key here (gsk_...)" Margin="0,0,10,0" Height="28" VerticalContentAlignment="Center"/>
            <Button x:Name="AddKeyButton" Grid.Column="1" Content="Add Key" Click="AddKeyButton_Click" Width="80" Height="28"/>
        </Grid>

        <!-- Keys List -->
        <GroupBox Grid.Row="2" Header="Registered API Keys &amp; Daily Requests" Margin="0,0,0,10">
            <ListBox x:Name="KeysList" BorderThickness="0" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Key display -->
                            <TextBlock Grid.Column="0" Text="{Binding DisplayKey}" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/>
                            
                            <!-- Request Count -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding RequestCount}" FontWeight="Bold"/>
                                <TextBlock Text=" requests" Foreground="Gray"/>
                            </StackPanel>

                            <!-- Status dot -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,15,0" VerticalAlignment="Center">
                                <Ellipse Width="8" Height="8" Margin="0,0,5,0" VerticalAlignment="Center">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Fill" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Fill" Value="Green"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsRateLimited}" Value="True">
                                                    <Setter Property="Fill" Value="Orange"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="Gray">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Text" Value="Invalid"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Text" Value="Active"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsRateLimited}" Value="True">
                                                    <Setter Property="Text" Value="Rate Limited"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>

                            <!-- Actions -->
                            <Button Grid.Column="3" Content="Delete" Click="DeleteKeyButton_Click" Tag="{Binding}" Background="#E53935" Foreground="White" BorderThickness="0" Height="20" Width="60" FontSize="10" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- Bottom Action Row -->
        <Grid Grid.Row="3">
            <Button x:Name="CloseButton" Content="Close" Click="CloseButton_Click" HorizontalAlignment="Right" Width="85" Height="28"/>
        </Grid>
    </Grid>
</Controls:MetroWindow>
