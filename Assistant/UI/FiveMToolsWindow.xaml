<Controls:MetroWindow x:Class="Assistant.UI.FiveMToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="FiveM Tools &amp; Utilities" Height="365" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Header -->
        <TextBlock Grid.Row="0" Text="Automated FiveM ReShade Fix" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Accent}" Margin="0,0,0,10"/>

        <!-- Main Tools Area -->
        <GroupBox Grid.Row="1" Header="ReShade Setup">
            <StackPanel Margin="10">
                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Margin="0,0,0,12">
                    Automates setting up ReShade in FiveM by moving files into <Run FontWeight="Bold">FiveM.app\plugins</Run> and injecting the crash warning bypass key into <Run FontWeight="Bold">CitizenFX.ini</Run>.
                </TextBlock>

                <!-- Side-by-side Action Buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Button x:Name="Step1MoveFilesBtn" Content="1. Move ReShade Files" Width="170" Height="32" Margin="0,0,10,0" Click="Step1MoveFilesBtn_Click"/>
                    <Button x:Name="Step2EnableReShadeBtn" Content="2. Enable ReShade" Width="170" Height="32" Click="Step2EnableReShadeBtn_Click"/>
                </StackPanel>

                <!-- Dynamic Status Message Container -->
                <Border x:Name="StatusBorder" BorderThickness="1" CornerRadius="4" Padding="10" MinHeight="65" Background="#1E1E1E" BorderBrush="#333333">
                    <TextBlock x:Name="StatusMessageText" TextWrapping="Wrap" FontSize="12" FontWeight="SemiBold" Foreground="#00FF66"/>
                </Border>
            </StackPanel>
        </GroupBox>

        <!-- Bottom Action Bar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CloseBtn" Content="Close" Width="90" Height="28" Click="CloseBtn_Click"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
