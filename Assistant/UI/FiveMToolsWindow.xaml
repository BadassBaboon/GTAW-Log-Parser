<Controls:MetroWindow x:Class="Assistant.UI.FiveMToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="FiveM Tools &amp; Utilities" Height="310" Width="540"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Header -->
        <TextBlock Grid.Row="0" Text="Automated FiveM ReShade Setup" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.Accent}" Margin="0,0,0,10"/>

        <!-- Main Tools Area -->
        <GroupBox Grid.Row="1" Header="ReShade Setup">
            <StackPanel Margin="12">
                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Margin="0,0,0,14">
                    Automates setting up ReShade in FiveM by relocating files to <Run FontWeight="Bold">FiveM.app\plugins</Run> and configuring the machine hardware key in <Run FontWeight="Bold">CitizenFX.ini</Run>.
                </TextBlock>

                <!-- Single Smart Action Button -->
                <Button x:Name="SetupReShadeBtn" Content="Setup &amp; Enable ReShade" Height="34" Margin="0,0,0,14" FontWeight="Bold" FontSize="13" Click="SetupReShadeBtn_Click"/>

                <!-- Dynamic Status Message Container -->
                <Border x:Name="StatusBorder" BorderThickness="1" CornerRadius="4" Padding="10" MinHeight="55" Background="#1E1E1E" BorderBrush="#333333">
                    <TextBlock x:Name="StatusMessageText" TextWrapping="Wrap" FontSize="12" FontWeight="SemiBold" Foreground="#00FF66" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
        </GroupBox>

        <!-- Bottom Action Bar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CloseBtn" Content="Close" Width="90" Height="28" Click="CloseBtn_Click"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
