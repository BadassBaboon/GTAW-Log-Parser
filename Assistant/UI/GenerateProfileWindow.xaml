<Controls:MetroWindow x:Class="Assistant.UI.GenerateProfileWindow"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      mc:Ignorable="d"
                      WindowStartupLocation="CenterOwner"
                      ResizeMode="NoResize"
                      BorderThickness="0"
                      GlowBrush="Black"
                      Title="AI Profile Generator" Height="500" Width="560" ShowInTaskbar="False" IsMinButtonEnabled="False" IsMaxRestoreButtonEnabled="False" ShowIconOnTitleBar="False" TitleCharacterCasing="Normal" Icon="../Resources/AppIcon.ico">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="110"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Section 1: Input Story -->
        <Label Grid.Row="0" Content="Describe your character's origin, background, personality, and speech style:" FontWeight="SemiBold" Padding="0,0,0,5"/>
        <TextBox x:Name="BackstoryInput" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Controls:TextBoxHelper.Watermark="e.g. 40yo Irish-American mobster from South Boston. Cynical, aggressive, speaks fast, uses Boston slang like 'wicked', drops ending 'g's, never says 'capisce'." Margin="0,0,0,10"/>

        <!-- Action Button & Progress -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="GenerateBtn" Grid.Column="0" Content="Generate Accent Profile with AI" Click="GenerateBtn_Click" Height="30" FontWeight="Bold"/>
            <TextBlock x:Name="StatusLabel" Grid.Column="1" Text="Generating..." VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#555555" Visibility="Collapsed"/>
        </Grid>

        <!-- Section 2: Results Preview -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Extracted Accent Name:" FontWeight="SemiBold" Padding="0,0,0,2"/>
            <TextBox x:Name="ResultNameInput" Grid.Row="1" Height="26" VerticalContentAlignment="Center" Margin="0,0,0,8"/>

            <Label Grid.Row="2" Content="Extracted Custom Speech Directives:" FontWeight="SemiBold" Padding="0,0,0,2"/>
            <TextBox x:Name="ResultDirectivesInput" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <!-- Section 3: Bottom Action Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ApplyBtn" Grid.Column="1" Content="Apply to Profiles" Click="ApplyBtn_Click" Height="28" Width="130" IsEnabled="False" FontWeight="SemiBold"/>
            <Button x:Name="CancelBtn" Grid.Column="3" Content="Cancel" Click="CancelBtn_Click" Height="28" Width="85"/>
        </Grid>
    </Grid>
</Controls:MetroWindow>
